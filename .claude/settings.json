{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "[ \"$(uname -s)\" = Linux ] || exit 0; cd \"$CLAUDE_PROJECT_DIR\" && if make -j4 >/tmp/sphere99-build.log 2>&1; then echo 'BUILD OK'; else echo 'BUILD FAILED:'; grep -E ': error:|Error [0-9]' /tmp/sphere99-build.log | head -20; fi"
          }
        ]
      }
    ]
  }
}
