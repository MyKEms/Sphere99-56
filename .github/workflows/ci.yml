name: CI

on:
  push:
    branches: [master]
  pull_request:

permissions:
  contents: read

jobs:
  build:
    name: Build (32-bit Linux)
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4

      - name: Install toolchain
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends g++-multilib make

      - name: Test CRefPtr null semantics
        run: python3 tools/test_refptr.py

      - name: Build
        run: make -j"$(nproc)" 2>&1 | tee build.log

      - name: Run container-list regression
        run: |
          make container-list-test
          build/container-list/container_list_test

      - name: Check binary
        run: file sphere99svr | grep -q 'ELF 32-bit'

      - name: Warning summary
        if: always()
        run: |
          {
            echo "### Compiler warnings"
            echo ""
            echo "Total: $(grep -c 'warning:' build.log || true)"
            echo ""
            echo '| count | kind |'
            echo '|---:|---|'
            grep -o '\[-W[a-z0-9+=-]*\]' build.log | sort | uniq -c | sort -rn | head -15 \
              | awk '{print "| " $1 " | `" $2 "` |"}'
          } >> "$GITHUB_STEP_SUMMARY"

  asan:
    name: Build (ASan/UBSan x86-64)
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4

      - name: Install toolchain
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends g++ make python3

      - name: Build sanitizer target
        run: make asan 2>&1 | tee asan-build.log

      - name: Check binary
        run: file build/asan/sphere99svr | grep -q 'ELF 64-bit'

      - name: Run container-list regression under ASan/UBSan
        env:
          ASAN_OPTIONS: abort_on_error=1:detect_leaks=0
          UBSAN_OPTIONS: halt_on_error=1:print_stacktrace=1
        run: |
          make container-list-asan-test
          build/asan-container-list/container_list_test

      - name: Verify crash recovery is opt-in
        run: |
          if nm -a build/asan/sphere99svr | grep -Eq 'sig(long)?jmp|CrashHandler'; then
            echo 'ASan build unexpectedly contains legacy crash recovery'
            exit 1
          fi

      - name: Run synthetic fixture under ASan/UBSan
        env:
          ASAN_OPTIONS: abort_on_error=1:detect_leaks=0
          UBSAN_OPTIONS: halt_on_error=1:print_stacktrace=1
        run: |
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-asan-probe" \
            --unknown-keyword-report \
            --unknown-keyword-probe
          python3 tools/fixtures/test_unknown_keyword_report.py \
            "$RUNNER_TEMP/sphere-asan-probe" \
            --binary "$GITHUB_WORKSPACE/build/asan/sphere99svr" \
            --port 2693
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-asan-normalization" \
            --unknown-keyword-report \
            --unknown-keyword-normalization-probe \
            --unknown-keyword-set-probe \
            --unknown-keyword-admin-probe
          python3 tools/fixtures/test_unknown_keyword_report.py \
            "$RUNNER_TEMP/sphere-asan-normalization" \
            --binary "$GITHUB_WORKSPACE/build/asan/sphere99svr" \
            --port 2694 \
            --normalization \
            --expect-set \
            --expect-on-demand
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-asan-rejected" \
            --unknown-keyword-report \
            --unknown-keyword-rejected-probe
          python3 tools/fixtures/test_unknown_keyword_report.py \
            "$RUNNER_TEMP/sphere-asan-rejected" \
            --binary "$GITHUB_WORKSPACE/build/asan/sphere99svr" \
            --port 2698 \
            --rejected
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-asan-disabled" \
            --unknown-keyword-probe
          python3 tools/fixtures/test_unknown_keyword_report.py \
            "$RUNNER_TEMP/sphere-asan-disabled" \
            --binary "$GITHUB_WORKSPACE/build/asan/sphere99svr" \
            --port 2695 \
            --expect-disabled
          python3 tools/fixtures/make_script_coverage_fixture.py \
            "$RUNNER_TEMP/sphere-asan-coverage-shutdown"
          python3 tools/fixtures/test_script_execution_coverage.py \
            "$RUNNER_TEMP/sphere-asan-coverage-shutdown" \
            --binary "$GITHUB_WORKSPACE/build/asan/sphere99svr" \
            --port 2700
          python3 tools/fixtures/make_script_coverage_fixture.py \
            "$RUNNER_TEMP/sphere-asan-coverage-on-demand" \
            --on-demand-report
          python3 tools/fixtures/test_script_execution_coverage.py \
            "$RUNNER_TEMP/sphere-asan-coverage-on-demand" \
            --binary "$GITHUB_WORKSPACE/build/asan/sphere99svr" \
            --port 2701 \
            --expect-on-demand
          python3 tools/fixtures/make_script_coverage_fixture.py \
            "$RUNNER_TEMP/sphere-asan-coverage-disabled" \
            --disabled \
            --on-demand-report
          python3 tools/fixtures/test_script_execution_coverage.py \
            "$RUNNER_TEMP/sphere-asan-coverage-disabled" \
            --binary "$GITHUB_WORKSPACE/build/asan/sphere99svr" \
            --port 2702 \
            --expect-disabled
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-asan-world-counts" \
            --world-load-counts \
            --world-load-counts-probe
          python3 tools/fixtures/test_world_load_counts.py \
            "$RUNNER_TEMP/sphere-asan-world-counts" \
            --binary "$GITHUB_WORKSPACE/build/asan/sphere99svr" \
            --port 2710
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-asan-world-counts-truncated" \
            --world-load-counts \
            --world-load-counts-probe \
            --truncate-world-item
          python3 tools/fixtures/test_world_load_counts.py \
            "$RUNNER_TEMP/sphere-asan-world-counts-truncated" \
            --binary "$GITHUB_WORKSPACE/build/asan/sphere99svr" \
            --port 2711 \
            --truncated
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-asan-world-counts-unresolved-char" \
            --world-load-counts \
            --world-load-counts-probe \
            --unresolved-worldchar-type
          python3 tools/fixtures/test_world_load_counts.py \
            "$RUNNER_TEMP/sphere-asan-world-counts-unresolved-char" \
            --binary "$GITHUB_WORKSPACE/build/asan/sphere99svr" \
            --port 2714 \
            --unresolved-worldchar-type
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-asan-world-counts-rejected-property" \
            --world-load-counts \
            --rejected-property
          python3 tools/fixtures/test_world_load_counts.py \
            "$RUNNER_TEMP/sphere-asan-world-counts-rejected-property" \
            --binary "$GITHUB_WORKSPACE/build/asan/sphere99svr" \
            --port 2722 \
            --rejected-property
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-asan-world-counts-weird-item" \
            --world-load-counts \
            --weird-item
          python3 tools/fixtures/test_world_load_counts.py \
            "$RUNNER_TEMP/sphere-asan-world-counts-weird-item" \
            --binary "$GITHUB_WORKSPACE/build/asan/sphere99svr" \
            --port 2723 \
            --weird-item
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-asan-world-counts-noncontainer" \
            --world-load-counts \
            --noncontainer-reference
          python3 tools/fixtures/test_world_load_counts.py \
            "$RUNNER_TEMP/sphere-asan-world-counts-noncontainer" \
            --binary "$GITHUB_WORKSPACE/build/asan/sphere99svr" \
            --port 2716 \
            --noncontainer-reference
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-asan-world-counts-typedef-container" \
            --world-load-counts \
            --typedef-container-reference
          python3 tools/fixtures/test_world_load_counts.py \
            "$RUNNER_TEMP/sphere-asan-world-counts-typedef-container" \
            --binary "$GITHUB_WORKSPACE/build/asan/sphere99svr" \
            --port 2717 \
            --typedef-container-reference
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-asan-world-counts-multi-property" \
            --world-load-counts \
            --multi-property
          python3 tools/fixtures/test_world_load_counts.py \
            "$RUNNER_TEMP/sphere-asan-world-counts-multi-property" \
            --binary "$GITHUB_WORKSPACE/build/asan/sphere99svr" \
            --port 2718 \
            --multi-property
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-asan-named-item-names" \
            --world-load-counts \
            --named-item-names
          python3 tools/fixtures/test_item_name_lifetime.py \
            "$RUNNER_TEMP/sphere-asan-named-item-names" \
            --binary "$GITHUB_WORKSPACE/build/asan/sphere99svr" \
            --port 2724
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-asan-named-resource-ids" \
            --world-load-counts \
            --named-resource-ids
          python3 tools/fixtures/test_named_resource_ids.py \
            "$RUNNER_TEMP/sphere-asan-named-resource-ids" \
            --binary "$GITHUB_WORKSPACE/build/asan/sphere99svr"
          python3 tools/fixtures/test_world_load_counts.py \
            "$RUNNER_TEMP/sphere-asan-named-resource-ids" \
            --binary "$GITHUB_WORKSPACE/build/asan/sphere99svr" \
            --port 2719 \
            --named-container-reference
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-asan-world-counts-child-before-parent" \
            --world-load-counts \
            --child-before-parent
          python3 tools/fixtures/test_world_load_counts.py \
            "$RUNNER_TEMP/sphere-asan-world-counts-child-before-parent" \
            --binary "$GITHUB_WORKSPACE/build/asan/sphere99svr" \
            --port 2724 \
            --child-before-parent
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-asan-world-roundtrip" \
            --world-load-counts \
            --child-before-parent \
            --world-save-probe \
            --roundtrip-integrity-probe
          python3 tools/fixtures/test_world_roundtrip_integrity.py \
            "$RUNNER_TEMP/sphere-asan-world-roundtrip" \
            --binary "$GITHUB_WORKSPACE/build/asan/sphere99svr" \
            --port 2727
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-asan-format-compat-counts" \
            --world-load-counts \
            --format-compat-probe \
            --world-save-probe \
            --roundtrip-integrity-probe
          python3 tools/fixtures/test_world_load_counts.py \
            "$RUNNER_TEMP/sphere-asan-format-compat-counts" \
            --binary "$GITHUB_WORKSPACE/build/asan/sphere99svr" \
            --port 2728 \
            --format-compat
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-asan-format-compat-roundtrip" \
            --world-load-counts \
            --format-compat-probe \
            --world-save-probe \
            --roundtrip-integrity-probe
          python3 tools/fixtures/test_world_roundtrip_integrity.py \
            "$RUNNER_TEMP/sphere-asan-format-compat-roundtrip" \
            --binary "$GITHUB_WORKSPACE/build/asan/sphere99svr" \
            --port 2729 \
            --format-compat
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-asan-events-attr" \
            --events-attr-probe
          ASAN_OPTIONS=quarantine_size_mb=64:malloc_context_size=8:detect_leaks=0:abort_on_error=1 \
          UBSAN_OPTIONS=halt_on_error=1:print_stacktrace=1 \
          python3 tools/fixtures/test_events_attr_roundtrip.py \
            "$RUNNER_TEMP/sphere-asan-events-attr" \
            --binary "$GITHUB_WORKSPACE/build/asan/sphere99svr" \
            --port 2735
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-asan-legacy-metadata" \
            --legacy-metadata-probe
          ASAN_OPTIONS=quarantine_size_mb=64:malloc_context_size=8:detect_leaks=0:abort_on_error=1 \
          UBSAN_OPTIONS=halt_on_error=1:print_stacktrace=1 \
          python3 tools/fixtures/test_legacy_metadata_roundtrip.py \
            "$RUNNER_TEMP/sphere-asan-legacy-metadata" \
            --binary "$GITHUB_WORKSPACE/build/asan/sphere99svr" \
            --port 2743
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-asan-metadata-roundtrip" \
            --world-load-counts \
            --metadata-roundtrip-probe \
            --world-save-probe \
            --roundtrip-integrity-probe
          python3 tools/fixtures/test_world_roundtrip_integrity.py \
            "$RUNNER_TEMP/sphere-asan-metadata-roundtrip" \
            --binary "$GITHUB_WORKSPACE/build/asan/sphere99svr" \
            --port 2744 \
            --metadata-roundtrip
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-asan-csv" \
            --unknown-keyword-report \
            --unknown-keyword-report-format csv \
            --unknown-keyword-probe
          python3 tools/fixtures/test_unknown_keyword_report.py \
            "$RUNNER_TEMP/sphere-asan-csv" \
            --binary "$GITHUB_WORKSPACE/build/asan/sphere99svr" \
            --port 2697 \
            --csv
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-asan-overflow" \
            --unknown-keyword-report \
            --unknown-keyword-overflow-probe
          python3 tools/fixtures/test_unknown_keyword_report.py \
            "$RUNNER_TEMP/sphere-asan-overflow" \
            --binary "$GITHUB_WORKSPACE/build/asan/sphere99svr" \
            --port 2696 \
            --overflow
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-asan-fixture" \
            --unknown-keyword-report
          python3 tools/fixtures/run_suite.py \
            "$RUNNER_TEMP/sphere-asan-fixture" \
            --binary "$GITHUB_WORKSPACE/build/asan/sphere99svr" \
            --repo "$GITHUB_WORKSPACE" \
            --port 2593 \
            --unknown-keyword-allowlist \
            "$GITHUB_WORKSPACE/tools/fixtures/unknown_keyword_allowlist.json"
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-asan-lifetime"
          python3 tools/fixtures/run_suite.py \
            "$RUNNER_TEMP/sphere-asan-lifetime" \
            --binary "$GITHUB_WORKSPACE/build/asan/sphere99svr" \
            --repo "$GITHUB_WORKSPACE" \
            --port 2793 \
            --lifetime-soak 25
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-asan-timer-lifetime" \
            --timer-lifetime-probe
          ASAN_OPTIONS=quarantine_size_mb=64:malloc_context_size=8:detect_leaks=0:abort_on_error=1 \
          python3 tools/fixtures/test_timer_lifetime.py \
            "$RUNNER_TEMP/sphere-asan-timer-lifetime" \
            --binary "$GITHUB_WORKSPACE/build/asan/sphere99svr" \
            --port 2794
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-asan-timer-lifetime-item-first" \
            --timer-lifetime-item-first-probe
          ASAN_OPTIONS=quarantine_size_mb=64:malloc_context_size=8:detect_leaks=0:abort_on_error=1 \
          python3 tools/fixtures/test_timer_lifetime.py \
            "$RUNNER_TEMP/sphere-asan-timer-lifetime-item-first" \
            --binary "$GITHUB_WORKSPACE/build/asan/sphere99svr" \
            --item-first \
            --port 2795
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-asan-timer-sibling-mutation" \
            --timer-sibling-mutation-probe
          ASAN_OPTIONS=quarantine_size_mb=64:malloc_context_size=8:detect_leaks=0:abort_on_error=1 \
          python3 tools/fixtures/test_timer_sibling_mutation.py \
            "$RUNNER_TEMP/sphere-asan-timer-sibling-mutation" \
            --binary "$GITHUB_WORKSPACE/build/asan/sphere99svr" \
            --port 2796
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-asan-timer-sibling-mutation-owner-first" \
            --timer-sibling-mutation-owner-first-probe
          ASAN_OPTIONS=quarantine_size_mb=64:malloc_context_size=8:detect_leaks=0:abort_on_error=1 \
          python3 tools/fixtures/test_timer_sibling_mutation.py \
            "$RUNNER_TEMP/sphere-asan-timer-sibling-mutation-owner-first" \
            --binary "$GITHUB_WORKSPACE/build/asan/sphere99svr" \
            --port 2797
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-asan-container-shutdown" \
            --container-shutdown-probe
          ASAN_OPTIONS=quarantine_size_mb=64:malloc_context_size=8:detect_leaks=0:abort_on_error=1 \
          UBSAN_OPTIONS=halt_on_error=1:print_stacktrace=1 \
          python3 tools/fixtures/test_container_shutdown.py \
            "$RUNNER_TEMP/sphere-asan-container-shutdown" \
            --binary "$GITHUB_WORKSPACE/build/asan/sphere99svr" \
            --port 2798
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-asan-ontick-content-mutation" \
            --ontick-content-mutation-probe
          ASAN_OPTIONS=quarantine_size_mb=64:malloc_context_size=8:detect_leaks=0:abort_on_error=1 \
          UBSAN_OPTIONS=halt_on_error=1:print_stacktrace=1 \
          python3 tools/fixtures/test_ontick_content_mutation.py \
            "$RUNNER_TEMP/sphere-asan-ontick-content-mutation" \
            --binary "$GITHUB_WORKSPACE/build/asan/sphere99svr" \
            --port 2799
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-asan-world-save" \
            --world-save-probe
          python3 tools/fixtures/test_world_save_roundtrip.py \
            "$RUNNER_TEMP/sphere-asan-world-save" \
            --binary "$GITHUB_WORKSPACE/build/asan/sphere99svr" \
            --port 2720
          python3 tools/fixtures/test_account_files.py \
            --binary "$GITHUB_WORKSPACE/build/asan/sphere99svr" \
            --port 2750
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-asan-spawn-gem" \
            --spawn-gem-probe \
            --world-save-probe
          python3 tools/fixtures/test_spawn_gem_serialization.py \
            "$RUNNER_TEMP/sphere-asan-spawn-gem" \
            --binary "$GITHUB_WORKSPACE/build/asan/sphere99svr" \
            --port 2740
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-asan-spawn-gem-duplicate" \
            --spawn-gem-duplicate-serial-probe
          python3 tools/fixtures/test_spawn_gem_serialization.py \
            "$RUNNER_TEMP/sphere-asan-spawn-gem-duplicate" \
            --binary "$GITHUB_WORKSPACE/build/asan/sphere99svr" \
            --port 2741 \
            --duplicate-load
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-asan-spawn-point" \
            --spawn-point-probe
          ASAN_OPTIONS=quarantine_size_mb=64:malloc_context_size=8:detect_leaks=0:abort_on_error=1 \
          UBSAN_OPTIONS=halt_on_error=1:print_stacktrace=1 \
          python3 tools/fixtures/test_spawn_point.py \
            "$RUNNER_TEMP/sphere-asan-spawn-point" \
            --binary "$GITHUB_WORKSPACE/build/asan/sphere99svr" \
            --port 2744
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-asan-dotted-expressions" \
            --dotted-expression-probe \
            --unknown-keyword-report
          python3 tools/fixtures/test_dotted_expressions.py \
            "$RUNNER_TEMP/sphere-asan-dotted-expressions" \
            --binary "$GITHUB_WORKSPACE/build/asan/sphere99svr" \
            --port 2731
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-asan-arg-locals" \
            --arg-locals-probe
          ASAN_OPTIONS=quarantine_size_mb=64:malloc_context_size=8:detect_leaks=0:abort_on_error=1 \
          python3 tools/fixtures/test_arg_locals.py \
            "$RUNNER_TEMP/sphere-asan-arg-locals" \
            --binary "$GITHUB_WORKSPACE/build/asan/sphere99svr" \
            --port 2732
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-asan-dword-hex" \
            --dword-hex-probe
          ASAN_OPTIONS=quarantine_size_mb=64:malloc_context_size=8:detect_leaks=0:abort_on_error=1 \
          python3 tools/fixtures/test_dword_hex.py \
            "$RUNNER_TEMP/sphere-asan-dword-hex" \
            --binary "$GITHUB_WORKSPACE/build/asan/sphere99svr" \
            --port 2738
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-asan-region-weather" \
            --region-weather-probe \
            --unknown-keyword-report
          ASAN_OPTIONS=quarantine_size_mb=64:malloc_context_size=8:detect_leaks=0:abort_on_error=1 \
          UBSAN_OPTIONS=halt_on_error=1:print_stacktrace=1 \
          python3 tools/fixtures/test_region_weather.py \
            "$RUNNER_TEMP/sphere-asan-region-weather" \
            --binary "$GITHUB_WORKSPACE/build/asan/sphere99svr" \
            --port 2750
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-asan-book-pages" \
            --book-pages-probe
          # The client session runs in the i386 job: this build does not
          # find the saved ground item by UID when it is double-clicked.
          python3 tools/fixtures/test_book_pages.py \
            "$RUNNER_TEMP/sphere-asan-book-pages" \
            --binary "$GITHUB_WORKSPACE/build/asan/sphere99svr" \
            --load-only
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-asan-dialog-buttons" \
            --dialog-button-probe
          python3 tools/fixtures/test_dialog_buttons.py \
            "$RUNNER_TEMP/sphere-asan-dialog-buttons" \
            --binary "$GITHUB_WORKSPACE/build/asan/sphere99svr" \
            --port 2734
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-asan-dialog-argo-layout" \
            --dialog-argo-layout-probe
          python3 tools/fixtures/test_dialog_buttons.py \
            "$RUNNER_TEMP/sphere-asan-dialog-argo-layout" \
            --binary "$GITHUB_WORKSPACE/build/asan/sphere99svr" \
            --port 2736 \
            --argo-layout
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-asan-dialog-flow-layout" \
            --dialog-flow-layout-probe
          python3 tools/fixtures/test_dialog_buttons.py \
            "$RUNNER_TEMP/sphere-asan-dialog-flow-layout" \
            --binary "$GITHUB_WORKSPACE/build/asan/sphere99svr" \
            --port 2737 \
            --flow-layout
          python3 tools/fixtures/make_gump_skill_fixture.py \
            "$RUNNER_TEMP/sphere-asan-gump-skill"
          ASAN_OPTIONS=quarantine_size_mb=64:malloc_context_size=8:detect_leaks=0:abort_on_error=1 \
          python3 tools/fixtures/test_gump_skill.py \
            "$RUNNER_TEMP/sphere-asan-gump-skill" \
            --binary "$GITHUB_WORKSPACE/build/asan/sphere99svr" \
            --port 2735

      - name: Upload ASan fixture server log on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: asan-fixture-server-log
          path: |
            ${{ runner.temp }}/sphere-asan-fixture/server.log
            ${{ runner.temp }}/sphere-asan-lifetime/server.log
            ${{ runner.temp }}/sphere-asan-coverage-shutdown/server.log
            ${{ runner.temp }}/sphere-asan-coverage-on-demand/server.log
            ${{ runner.temp }}/sphere-asan-coverage-disabled/server.log
            ${{ runner.temp }}/sphere-asan-world-counts/server.log
            ${{ runner.temp }}/sphere-asan-world-counts-truncated/server.log
            ${{ runner.temp }}/sphere-asan-world-counts-rejected-property/server.log
            ${{ runner.temp }}/sphere-asan-world-counts-weird-item/server.log
            ${{ runner.temp }}/sphere-asan-named-item-names/server.log
            ${{ runner.temp }}/sphere-asan-named-resource-ids/server.log
            ${{ runner.temp }}/sphere-asan-timer-lifetime/server.log
            ${{ runner.temp }}/sphere-asan-timer-lifetime-item-first/server.log
            ${{ runner.temp }}/sphere-asan-timer-sibling-mutation/server.log
            ${{ runner.temp }}/sphere-asan-timer-sibling-mutation-owner-first/server.log
            ${{ runner.temp }}/sphere-asan-container-shutdown/server.log
            ${{ runner.temp }}/sphere-asan-ontick-content-mutation/server.log
            ${{ runner.temp }}/sphere-asan-world-save/server.log
            ${{ runner.temp }}/sphere-asan-spawn-gem/server.log
            ${{ runner.temp }}/sphere-asan-spawn-gem/server-reload.log
            ${{ runner.temp }}/sphere-asan-spawn-gem-duplicate/server.log
            ${{ runner.temp }}/sphere-asan-spawn-point/server.log
            ${{ runner.temp }}/sphere-asan-dotted-expressions/server.log
            ${{ runner.temp }}/sphere-asan-arg-locals/server.log
            ${{ runner.temp }}/sphere-asan-dword-hex/server.log
            ${{ runner.temp }}/sphere-asan-region-weather/server.log
            ${{ runner.temp }}/sphere-asan-metadata-roundtrip/server.log
            ${{ runner.temp }}/sphere-asan-metadata-roundtrip/server-round-1.log
            ${{ runner.temp }}/sphere-asan-metadata-roundtrip/server-round-2.log
            ${{ runner.temp }}/sphere-asan-dialog-buttons/server.log
            ${{ runner.temp }}/sphere-asan-dialog-argo-layout/server.log
            ${{ runner.temp }}/sphere-asan-dialog-flow-layout/server.log
            ${{ runner.temp }}/sphere-asan-gump-skill/server.log
            ${{ runner.temp }}/sphere-asan-events-attr/events-attr-generation-*.log
          if-no-files-found: warn

  load-safety:
    name: World-load save guard
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4

      - name: Install toolchain
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends g++-multilib make

      - name: Run disposable broken-world regression
        run: |
          make load-safety-test
          build/load-safety/load_safety_test

      - name: Run checked-save I/O fault injection
        run: |
          make save-io-test
          build/save-io/save_io_test

  value-ranges:
    name: Value-range properties and script lookup tables
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4

      - name: Install toolchain
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends g++-multilib make

      - name: Run offline regressions
        run: |
          make value-range-test
          build/value-range/value_range_test
          python3 tools/fixtures/test_resource_type_ids.py

  lint:
    name: Lint tools
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4

      - name: Test fixture shutdown gate
        run: python3 tools/fixtures/test_run_suite.py

      - name: Python syntax
        run: python3 -m py_compile tools/*.py tools/fixtures/*.py

      - name: Public boundary and secret scan
        run: python3 tools/check_secrets.py --all

      - name: Offline protocol fixtures
        run: python3 tools/test_protocol.py

      - name: External denylist unit checks
        run: python3 tools/test_secret_denylist.py

      - name: shellcheck
        run: shellcheck tools/*.sh

  fixture:
    name: Headless synthetic fixture (32-bit Linux)
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4

      - name: Install toolchain
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends g++-multilib make python3

      - name: Build i386 server
        run: make -j"$(nproc)"

      - name: Generate fixture and run protocol suite
        run: |
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-probe" \
            --unknown-keyword-report \
            --unknown-keyword-probe
          python3 tools/fixtures/test_unknown_keyword_report.py \
            "$RUNNER_TEMP/sphere-probe" \
            --binary "$GITHUB_WORKSPACE/sphere99svr" \
            --port 2693
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-normalization" \
            --unknown-keyword-report \
            --unknown-keyword-normalization-probe \
            --unknown-keyword-set-probe \
            --unknown-keyword-admin-probe
          python3 tools/fixtures/test_unknown_keyword_report.py \
            "$RUNNER_TEMP/sphere-normalization" \
            --binary "$GITHUB_WORKSPACE/sphere99svr" \
            --port 2694 \
            --normalization \
            --expect-set \
            --expect-on-demand
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-rejected" \
            --unknown-keyword-report \
            --unknown-keyword-rejected-probe
          python3 tools/fixtures/test_unknown_keyword_report.py \
            "$RUNNER_TEMP/sphere-rejected" \
            --binary "$GITHUB_WORKSPACE/sphere99svr" \
            --port 2698 \
            --rejected
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-disabled" \
            --unknown-keyword-probe
          python3 tools/fixtures/test_unknown_keyword_report.py \
            "$RUNNER_TEMP/sphere-disabled" \
            --binary "$GITHUB_WORKSPACE/sphere99svr" \
            --port 2695 \
            --expect-disabled
          python3 tools/fixtures/make_script_coverage_fixture.py \
            "$RUNNER_TEMP/sphere-coverage-shutdown"
          python3 tools/fixtures/test_script_execution_coverage.py \
            "$RUNNER_TEMP/sphere-coverage-shutdown" \
            --binary "$GITHUB_WORKSPACE/sphere99svr" \
            --port 2700
          python3 tools/fixtures/make_script_coverage_fixture.py \
            "$RUNNER_TEMP/sphere-coverage-on-demand" \
            --on-demand-report
          python3 tools/fixtures/test_script_execution_coverage.py \
            "$RUNNER_TEMP/sphere-coverage-on-demand" \
            --binary "$GITHUB_WORKSPACE/sphere99svr" \
            --port 2701 \
            --expect-on-demand
          python3 tools/fixtures/make_script_coverage_fixture.py \
            "$RUNNER_TEMP/sphere-coverage-disabled" \
            --disabled \
            --on-demand-report
          python3 tools/fixtures/test_script_execution_coverage.py \
            "$RUNNER_TEMP/sphere-coverage-disabled" \
            --binary "$GITHUB_WORKSPACE/sphere99svr" \
            --port 2702 \
            --expect-disabled
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-world-counts" \
            --world-load-counts \
            --world-load-counts-probe
          python3 tools/fixtures/test_world_load_counts.py \
            "$RUNNER_TEMP/sphere-world-counts" \
            --binary "$GITHUB_WORKSPACE/sphere99svr" \
            --port 2712
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-world-counts-truncated" \
            --world-load-counts \
            --world-load-counts-probe \
            --truncate-world-item
          python3 tools/fixtures/test_world_load_counts.py \
            "$RUNNER_TEMP/sphere-world-counts-truncated" \
            --binary "$GITHUB_WORKSPACE/sphere99svr" \
            --port 2713 \
            --truncated
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-world-counts-unresolved-char" \
            --world-load-counts \
            --world-load-counts-probe \
            --unresolved-worldchar-type
          python3 tools/fixtures/test_world_load_counts.py \
            "$RUNNER_TEMP/sphere-world-counts-unresolved-char" \
            --binary "$GITHUB_WORKSPACE/sphere99svr" \
            --port 2715 \
            --unresolved-worldchar-type
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-world-counts-rejected-property" \
            --world-load-counts \
            --rejected-property
          python3 tools/fixtures/test_world_load_counts.py \
            "$RUNNER_TEMP/sphere-world-counts-rejected-property" \
            --binary "$GITHUB_WORKSPACE/sphere99svr" \
            --port 2722 \
            --rejected-property
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-world-counts-weird-item" \
            --world-load-counts \
            --weird-item
          python3 tools/fixtures/test_world_load_counts.py \
            "$RUNNER_TEMP/sphere-world-counts-weird-item" \
            --binary "$GITHUB_WORKSPACE/sphere99svr" \
            --port 2723 \
            --weird-item
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-world-counts-noncontainer" \
            --world-load-counts \
            --noncontainer-reference
          python3 tools/fixtures/test_world_load_counts.py \
            "$RUNNER_TEMP/sphere-world-counts-noncontainer" \
            --binary "$GITHUB_WORKSPACE/sphere99svr" \
            --port 2716 \
            --noncontainer-reference
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-world-counts-typedef-container" \
            --world-load-counts \
            --typedef-container-reference
          python3 tools/fixtures/test_world_load_counts.py \
            "$RUNNER_TEMP/sphere-world-counts-typedef-container" \
            --binary "$GITHUB_WORKSPACE/sphere99svr" \
            --port 2717 \
            --typedef-container-reference
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-world-counts-multi-property" \
            --world-load-counts \
            --multi-property
          python3 tools/fixtures/test_world_load_counts.py \
            "$RUNNER_TEMP/sphere-world-counts-multi-property" \
            --binary "$GITHUB_WORKSPACE/sphere99svr" \
            --port 2718 \
            --multi-property
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-named-item-names" \
            --world-load-counts \
            --named-item-names
          python3 tools/fixtures/test_item_name_lifetime.py \
            "$RUNNER_TEMP/sphere-named-item-names" \
            --binary "$GITHUB_WORKSPACE/sphere99svr" \
            --port 2724
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-named-resource-ids" \
            --world-load-counts \
            --named-resource-ids
          python3 tools/fixtures/test_named_resource_ids.py \
            "$RUNNER_TEMP/sphere-named-resource-ids" \
            --binary "$GITHUB_WORKSPACE/sphere99svr"
          python3 tools/fixtures/test_world_load_counts.py \
            "$RUNNER_TEMP/sphere-named-resource-ids" \
            --binary "$GITHUB_WORKSPACE/sphere99svr" \
            --port 2719 \
            --named-container-reference
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-world-counts-child-before-parent" \
            --world-load-counts \
            --child-before-parent
          python3 tools/fixtures/test_world_load_counts.py \
            "$RUNNER_TEMP/sphere-world-counts-child-before-parent" \
            --binary "$GITHUB_WORKSPACE/sphere99svr" \
            --port 2724 \
            --child-before-parent
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-world-roundtrip" \
            --world-load-counts \
            --child-before-parent \
            --world-save-probe \
            --roundtrip-integrity-probe
          python3 tools/fixtures/test_world_roundtrip_integrity.py \
            "$RUNNER_TEMP/sphere-world-roundtrip" \
            --binary "$GITHUB_WORKSPACE/sphere99svr" \
            --port 2727
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-format-compat-counts" \
            --world-load-counts \
            --format-compat-probe \
            --world-save-probe \
            --roundtrip-integrity-probe
          python3 tools/fixtures/test_world_load_counts.py \
            "$RUNNER_TEMP/sphere-format-compat-counts" \
            --binary "$GITHUB_WORKSPACE/sphere99svr" \
            --port 2728 \
            --format-compat
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-format-compat-roundtrip" \
            --world-load-counts \
            --format-compat-probe \
            --world-save-probe \
            --roundtrip-integrity-probe
          python3 tools/fixtures/test_world_roundtrip_integrity.py \
            "$RUNNER_TEMP/sphere-format-compat-roundtrip" \
            --binary "$GITHUB_WORKSPACE/sphere99svr" \
            --port 2729 \
            --format-compat
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-events-attr" \
            --events-attr-probe
          python3 tools/fixtures/test_events_attr_roundtrip.py \
            "$RUNNER_TEMP/sphere-events-attr" \
            --binary "$GITHUB_WORKSPACE/sphere99svr" \
            --port 2735
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-legacy-metadata" \
            --legacy-metadata-probe
          python3 tools/fixtures/test_legacy_metadata_roundtrip.py \
            "$RUNNER_TEMP/sphere-legacy-metadata" \
            --binary "$GITHUB_WORKSPACE/sphere99svr" \
            --port 2742
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-metadata-roundtrip" \
            --world-load-counts \
            --metadata-roundtrip-probe \
            --world-save-probe \
            --roundtrip-integrity-probe
          python3 tools/fixtures/test_world_roundtrip_integrity.py \
            "$RUNNER_TEMP/sphere-metadata-roundtrip" \
            --binary "$GITHUB_WORKSPACE/sphere99svr" \
            --port 2745 \
            --metadata-roundtrip
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-csv" \
            --unknown-keyword-report \
            --unknown-keyword-report-format csv \
            --unknown-keyword-probe
          python3 tools/fixtures/test_unknown_keyword_report.py \
            "$RUNNER_TEMP/sphere-csv" \
            --binary "$GITHUB_WORKSPACE/sphere99svr" \
            --port 2697 \
            --csv
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-overflow" \
            --unknown-keyword-report \
            --unknown-keyword-overflow-probe
          python3 tools/fixtures/test_unknown_keyword_report.py \
            "$RUNNER_TEMP/sphere-overflow" \
            --binary "$GITHUB_WORKSPACE/sphere99svr" \
            --port 2696 \
            --overflow
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-fixture" \
            --unknown-newbie \
            --unknown-keyword-report
          python3 tools/fixtures/run_suite.py \
            "$RUNNER_TEMP/sphere-fixture" \
            --binary "$GITHUB_WORKSPACE/sphere99svr" \
            --repo "$GITHUB_WORKSPACE" \
            --port 2593 \
            --expect-invalid-newbie SYNTHETIC_UNKNOWN_SKILL \
            --unknown-keyword-allowlist \
            "$GITHUB_WORKSPACE/tools/fixtures/unknown_keyword_allowlist.json"
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-timer-lifetime" \
            --timer-lifetime-probe
          python3 tools/fixtures/test_timer_lifetime.py \
            "$RUNNER_TEMP/sphere-timer-lifetime" \
            --binary "$GITHUB_WORKSPACE/sphere99svr" \
            --port 2794
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-timer-lifetime-item-first" \
            --timer-lifetime-item-first-probe
          python3 tools/fixtures/test_timer_lifetime.py \
            "$RUNNER_TEMP/sphere-timer-lifetime-item-first" \
            --binary "$GITHUB_WORKSPACE/sphere99svr" \
            --item-first \
            --port 2795
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-timer-sibling-mutation" \
            --timer-sibling-mutation-probe
          python3 tools/fixtures/test_timer_sibling_mutation.py \
            "$RUNNER_TEMP/sphere-timer-sibling-mutation" \
            --binary "$GITHUB_WORKSPACE/sphere99svr" \
            --port 2796
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-timer-sibling-mutation-owner-first" \
            --timer-sibling-mutation-owner-first-probe
          python3 tools/fixtures/test_timer_sibling_mutation.py \
            "$RUNNER_TEMP/sphere-timer-sibling-mutation-owner-first" \
            --binary "$GITHUB_WORKSPACE/sphere99svr" \
            --port 2797
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-container-shutdown" \
            --container-shutdown-probe
          python3 tools/fixtures/test_container_shutdown.py \
            "$RUNNER_TEMP/sphere-container-shutdown" \
            --binary "$GITHUB_WORKSPACE/sphere99svr" \
            --port 2798
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-ontick-content-mutation" \
            --ontick-content-mutation-probe
          python3 tools/fixtures/test_ontick_content_mutation.py \
            "$RUNNER_TEMP/sphere-ontick-content-mutation" \
            --binary "$GITHUB_WORKSPACE/sphere99svr" \
            --port 2799
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-world-save" \
            --world-save-probe
          python3 tools/fixtures/test_world_save_roundtrip.py \
            "$RUNNER_TEMP/sphere-world-save" \
            --binary "$GITHUB_WORKSPACE/sphere99svr" \
            --port 2720
          python3 tools/fixtures/test_account_files.py \
            --binary "$GITHUB_WORKSPACE/sphere99svr" \
            --port 2751
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-spawn-gem" \
            --spawn-gem-probe \
            --world-save-probe
          python3 tools/fixtures/test_spawn_gem_serialization.py \
            "$RUNNER_TEMP/sphere-spawn-gem" \
            --binary "$GITHUB_WORKSPACE/sphere99svr" \
            --port 2742
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-spawn-gem-duplicate" \
            --spawn-gem-duplicate-serial-probe
          python3 tools/fixtures/test_spawn_gem_serialization.py \
            "$RUNNER_TEMP/sphere-spawn-gem-duplicate" \
            --binary "$GITHUB_WORKSPACE/sphere99svr" \
            --port 2743 \
            --duplicate-load
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-spawn-point" \
            --spawn-point-probe
          python3 tools/fixtures/test_spawn_point.py \
            "$RUNNER_TEMP/sphere-spawn-point" \
            --binary "$GITHUB_WORKSPACE/sphere99svr" \
            --port 2745
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-dotted-expressions" \
            --dotted-expression-probe \
            --unknown-keyword-report
          python3 tools/fixtures/test_dotted_expressions.py \
            "$RUNNER_TEMP/sphere-dotted-expressions" \
            --binary "$GITHUB_WORKSPACE/sphere99svr" \
            --port 2730
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-arg-locals" \
            --arg-locals-probe
          python3 tools/fixtures/test_arg_locals.py \
            "$RUNNER_TEMP/sphere-arg-locals" \
            --binary "$GITHUB_WORKSPACE/sphere99svr" \
            --port 2732
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-dword-hex" \
            --dword-hex-probe
          python3 tools/fixtures/test_dword_hex.py \
            "$RUNNER_TEMP/sphere-dword-hex" \
            --binary "$GITHUB_WORKSPACE/sphere99svr" \
            --port 2739
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-region-weather" \
            --region-weather-probe \
            --unknown-keyword-report
          python3 tools/fixtures/test_region_weather.py \
            "$RUNNER_TEMP/sphere-region-weather" \
            --binary "$GITHUB_WORKSPACE/sphere99svr" \
            --port 2751
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-book-pages" \
            --book-pages-probe
          python3 tools/fixtures/test_book_pages.py \
            "$RUNNER_TEMP/sphere-book-pages" \
            --binary "$GITHUB_WORKSPACE/sphere99svr" \
            --port 2733
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-dialog-buttons" \
            --dialog-button-probe
          python3 tools/fixtures/test_dialog_buttons.py \
            "$RUNNER_TEMP/sphere-dialog-buttons" \
            --binary "$GITHUB_WORKSPACE/sphere99svr" \
            --port 2734
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-dialog-argo-layout" \
            --dialog-argo-layout-probe
          python3 tools/fixtures/test_dialog_buttons.py \
            "$RUNNER_TEMP/sphere-dialog-argo-layout" \
            --binary "$GITHUB_WORKSPACE/sphere99svr" \
            --port 2736 \
            --argo-layout
          python3 tools/fixtures/make_fixture.py \
            "$RUNNER_TEMP/sphere-dialog-flow-layout" \
            --dialog-flow-layout-probe
          python3 tools/fixtures/test_dialog_buttons.py \
            "$RUNNER_TEMP/sphere-dialog-flow-layout" \
            --binary "$GITHUB_WORKSPACE/sphere99svr" \
            --port 2737 \
            --flow-layout
          python3 tools/fixtures/make_gump_skill_fixture.py \
            "$RUNNER_TEMP/sphere-gump-skill"
          python3 tools/fixtures/test_gump_skill.py \
            "$RUNNER_TEMP/sphere-gump-skill" \
            --binary "$GITHUB_WORKSPACE/sphere99svr" \
            --port 2735

      - name: Upload server log on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: synthetic-fixture-server-log
          path: |
            ${{ runner.temp }}/sphere-fixture/server.log
            ${{ runner.temp }}/sphere-coverage-shutdown/server.log
            ${{ runner.temp }}/sphere-coverage-on-demand/server.log
            ${{ runner.temp }}/sphere-coverage-disabled/server.log
            ${{ runner.temp }}/sphere-world-counts/server.log
            ${{ runner.temp }}/sphere-world-counts-truncated/server.log
            ${{ runner.temp }}/sphere-world-counts-rejected-property/server.log
            ${{ runner.temp }}/sphere-world-counts-weird-item/server.log
            ${{ runner.temp }}/sphere-named-item-names/server.log
            ${{ runner.temp }}/sphere-timer-lifetime/server.log
            ${{ runner.temp }}/sphere-timer-lifetime-item-first/server.log
            ${{ runner.temp }}/sphere-timer-sibling-mutation/server.log
            ${{ runner.temp }}/sphere-timer-sibling-mutation-owner-first/server.log
            ${{ runner.temp }}/sphere-container-shutdown/server.log
            ${{ runner.temp }}/sphere-ontick-content-mutation/server.log
            ${{ runner.temp }}/sphere-world-save/server.log
            ${{ runner.temp }}/sphere-spawn-gem/server.log
            ${{ runner.temp }}/sphere-spawn-gem/server-reload.log
            ${{ runner.temp }}/sphere-spawn-gem-duplicate/server.log
            ${{ runner.temp }}/sphere-spawn-point/server.log
            ${{ runner.temp }}/sphere-dotted-expressions/server.log
            ${{ runner.temp }}/sphere-arg-locals/server.log
            ${{ runner.temp }}/sphere-dword-hex/server.log
            ${{ runner.temp }}/sphere-metadata-roundtrip/server.log
            ${{ runner.temp }}/sphere-metadata-roundtrip/server-round-1.log
            ${{ runner.temp }}/sphere-metadata-roundtrip/server-round-2.log
            ${{ runner.temp }}/sphere-book-pages/server.log
            ${{ runner.temp }}/sphere-dialog-buttons/server.log
            ${{ runner.temp }}/sphere-dialog-argo-layout/server.log
            ${{ runner.temp }}/sphere-dialog-flow-layout/server.log
            ${{ runner.temp }}/sphere-gump-skill/server.log
          if-no-files-found: warn
